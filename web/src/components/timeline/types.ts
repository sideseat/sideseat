export interface TimeScaleTick {
  position: number;
  label: string;
}

export interface TimelineMetrics {
  barWidth: number; // bar width in pixels
  marginLeft: number; // margin from left (time-based)
}
