{
  "id": "traceloop",
  "doc": "The TraceLoop dialect.",
  "carriers": [
    {
      "id": "traceloop.entity_input",
      "doc": "What the entity received.",
      "match": {
        "attribute": "traceloop.entity.input"
      },
      "facts": {
        "preset": "snapshot",
        "carrier_holds_span_input": true
      }
    },
    {
      "id": "traceloop.entity_output",
      "doc": "What it produced.",
      "match": {
        "attribute": "traceloop.entity.output"
      },
      "facts": {
        "preset": "snapshot",
        "carrier_holds_span_output": true
      }
    }
  ],
  "detect": [
    {
      "id": "traceloop.detect",
      "doc": "Identifies TraceLoop by the signals below.",
      "label": "TraceLoop",
      "match": {
        "attr_prefix": [
          "traceloop."
        ],
        "resource_attr_contains": [
          {
            "key": "telemetry.sdk.name",
            "value": "traceloop"
          }
        ]
      },
      "legacy_rank": 220
    }
  ],
  "sdk_slugs": [
    {
      "slug": "traceloop",
      "label": "TraceLoop"
    }
  ],
  "messages": [
    {
      "id": "traceloop.entity_input",
      "doc": "TraceLoop reports a span's whole input under one key, as a JSON string.",
      "read": {
        "attribute": "traceloop.entity.input"
      },
      "parse": "json",
      "emit": "message",
      "legacy_rank": 1000
    },
    {
      "id": "traceloop.entity_output",
      "doc": "And its whole output under another.",
      "read": {
        "attribute": "traceloop.entity.output"
      },
      "parse": "json",
      "emit": "message",
      "legacy_rank": 1010
    }
  ]
}
